version: '3.5'
services:
  api:
    container_name: $MODEL_CONTAINER
    image: $MODEL_IMAGE
    build:
      context: .
    restart: always
    ports:
      - $MODEL_PORT:$MODEL_PORT
    expose:
      - $MODEL_PORT
    volumes:
      - $MODEL_VOLUME
    env_file:
      - .env
    depends_on:
      - db
  db:
    image: $MONGO_IMAGE
    container_name: $MONGO_CONTAINER
    env_file:
      - .env
    environment:
      VIRTUAL_HOST: $MONGO_CONTAINER
    expose:
      - $MONGO_PORT
networks:
  default:
    name: $NETWORK 
